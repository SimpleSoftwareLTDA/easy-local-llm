# 2026-04-04

- User requested to change the color of the footer text "Easy Local LLM runs entirely on your local machine." to white.
- Found the text in `ell/src/mainview/App.tsx` on line 193.
- Changed class `text-zinc-700` to `text-zinc-100` (premium white) to match the app's dark theme.
- Fixed the issue where LLM output was not rendering as markdown. 
- Installed `react-markdown` and `@tailwindcss/typography`.
- Configured tailwind.config.js to use the typography plugin.
- Rendered the LLM response in `ell/src/mainview/App.tsx` using `<ReactMarkdown>` wrapped in a `div` with `prose prose-zinc prose-invert` for excellent styling.
- Updated README.md with Ray Edwards' PASTOR copywriting framework, emphasizing privacy, zero-config, and performance.
- Optimized README for SEO (Local LLM, Private AI) and AEO (FAQ for AI search engines).
- Replaced the Electrobun template placeholder with actual ELL (Easy Local LLM) functionality details.
- Created PRINCIPLES.md with global writing guidelines.
- Fixed the TypeScript "not callable" error in `App.tsx` by refactoring the `handleSend` function to extract the system guidelines and fetch payload into variables.
- Corrected a typo "With is is guidelines" to "With these guidelines" in the system guidelines string.
- Set the default state of "Deep Think" to disabled (false) as requested by the user.
- Resolved TypeScript import error for `.ico` files by creating `src/mainview/vite-env.d.ts` with appropriate type declarations.
- Verified that the "Deep Think" toggle reflects the initial state correctly in the UI.

- Updated the application's visual identity by replacing Sparkles (✨) and Feather (🪶) emojis with the official `assets/icon.png` in the startup loader, main header, and initial empty-state dashboard.
- Ensured consistent branding across the entire `checking` and `ready` states.
- Cleaned and re-built the Windows executable in canary mode, confirming the icon fix automation works as expected.
- Restructured README.md following behavioral focus and bluntness principles.
- Replaced all long dashes (—) with semicolons (;) throughout the documentation.
